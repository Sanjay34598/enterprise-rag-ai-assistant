# Maintenance & Operational Automation Scripts

This directory contains standalone utility scripts for bootstrapping databases, populating test data, and initializing local environments.

## Scripts
- `seed_db.py`: Seeds initial admin user credentials and default test document metadata into PostgreSQL.
- `setup_env.sh`: Shell script initializing environment variables and pulling necessary local Ollama models.
