# Root Integration & End-to-End Test Suite

This directory contains repository-level End-to-End (E2E) integration tests and load testing scripts.

## Structure
- `e2e/`: Browser & multi-component workflow test scripts.
- Unit and component integration tests are located in their respective packages (`backend/tests/` and `frontend/src/`).
